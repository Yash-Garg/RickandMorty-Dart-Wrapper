name: test_rm_api
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  rick_and_morty_api:
    path: ../rick-and-morty-dart-api

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
